# Шрифты

Визуализация bbox в коде использует системный шрифт в Docker:
`/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf` (см. `kitmatch/utils/visualization.py`).

Папка `static/fonts/` предназначена для дополнительных статических файлов при необходимости.
